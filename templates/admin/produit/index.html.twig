{% extends 'base.html.twig' %}

{% block title %}Produits{% endblock %}

{% block body %}
    <h1>Produits</h1>
    <p><a href="{{ path('admin_produits_nouveau') }}" class="btn">+ Ajouter un produit</a></p>

    <table>
        <thead>
            <tr><th></th><th>Nom</th><th>Catégorie</th><th>Vente</th><th>Prix</th><th>Statut</th><th></th></tr>
        </thead>
        <tbody>
        {% for produit in produits %}
            <tr>
                <td>{% if produit.image %}<img class="miniature" src="/uploads/produits/{{ produit.image }}" alt="{{ produit.nom }}">{% endif %}</td>
                <td>{{ produit.nom }}</td>
                <td>{{ produit.categorie ? produit.categorie.nom : '—' }}</td>
                <td>{{ produit.estVenduAuKilo ? 'Au kilo (' ~ produit.variantes|length ~ ' variante(s))' : 'À la pièce' }}</td>
                <td>{{ produit.prix }} €{{ produit.estVenduAuKilo ? '/kg' : '' }}</td>
                <td><span class="badge {{ produit.actif ? 'actif' : 'inactif' }}">{{ produit.actif ? 'Actif' : 'Inactif' }}</span></td>
                <td>
                    <a href="{{ path('admin_produits_modifier', {id: produit.id}) }}" class="btn secondaire">Modifier</a>
                    <form class="inline" method="post" action="{{ path('admin_produits_supprimer', {id: produit.id}) }}" onsubmit="return confirm('Supprimer ce produit ?');">
                        <input type="hidden" name="_csrf_token" value="{{ csrf_token('supprimer_produit_' ~ produit.id) }}">
                        <button type="submit" class="btn danger">Supprimer</button>
                    </form>
                </td>
            </tr>
        {% else %}
            <tr><td colspan="7">Aucun produit pour le moment.</td></tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
